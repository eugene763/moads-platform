export const aeoAppDescription = {
  productCode: "aeo",
  role: "standalone pro checker",
  phase: "placeholder",
};

if (process.env.NODE_ENV !== "test") {
  console.log("[aeo-web] Placeholder scaffold ready.");
}
